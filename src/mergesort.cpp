/*
 * mergesort.cpp
 * 
 * Performs merge sort based on the intensity of the pixels
 * Used by median blur(median.cpp).
 */

#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include <iostream>
#include "mergesort.h"
#include "median.h"

using namespace std;

int merge(grayPixel_t A, int lo1, int hi1, int lo2, int hi2) {
    //Redacted
}

/* This is the function that is called */
int mergesort(grayPixel_t A, int lo, int hi) {
    //Redacted
}
